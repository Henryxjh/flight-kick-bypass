name: Release

on:
  push:
    tags:
      - 'v*.*.*-*.*.*-fabric-neoforge'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate release tag
        run: |
          if [[ ! "${GITHUB_REF_NAME}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+-[0-9]+\.[0-9]+\.[0-9]+-fabric-neoforge$ ]]; then
            echo "Invalid release tag: ${GITHUB_REF_NAME}"
            echo "Expected format: v<mod-version>-<minecraft-version>-fabric-neoforge"
            echo "Example: v1.0.1-1.21.1-fabric-neoforge"
            exit 1
          fi

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Build with Gradle
        run: ./gradlew build

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: flightkickbypass-${{ github.ref_name }}
          path: build/libs/flightkickbypass-*-fabric-neoforge-*.jar
          if-no-files-found: error

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "${{ github.ref_name }}" build/libs/flightkickbypass-*-fabric-neoforge-*.jar --generate-notes --verify-tag
